; SMTLIB2 export - path constraint
; Generated by PyExZ3
(set-logic QF_BV)

(declare-fun key () Int)

(assert (not (= (== (^ (+ key, (<< key, const)), (>> (+ key, (<< key, const)), const)), (+ key, const)) 0)))
; Predicate 0: (== (^ (+ key, (<< key, const)), (>> (+ key, (<< key, const)), const)), (+ key, const)) (result: False)
; Tree: ['raw', '(== (^ (+ key#0, (<< key#0, const#10)), (>> (+ key#0, (<< key#0, const#10)), const#6)), (+ key#0, const#1))']

(assert (not (= (== (^ (+ key, (<< key, const)), (>> (+ key, (<< key, const)), const)), (+ key, const)) 0)))
; Predicate 1: (== (^ (+ key, (<< key, const)), (>> (+ key, (<< key, const)), const)), (+ key, const)) (result: False)
; Tree: ['raw', '(== (^ (+ key#0, (<< key#0, const#10)), (>> (+ key#0, (<< key#0, const#10)), const#6)), (+ key#0, const#1))']

(check-sat)
(get-model)
