; SMTLIB2 export - path constraint
; Generated by PyExZ3
(set-logic QF_BV)

(declare-fun x () Int)
(declare-fun y () Int)

(assert (>= x 0))
; Predicate 0: (>= x, 0) (result: True)
; Tree: ['>=', 'x', 0]

(assert (>= y 0))
; Predicate 1: (>= y, 0) (result: True)
; Tree: ['>=', 'y', 0]

(assert (>= y 0))
; Predicate 2: (>= y, 0) (result: True)
; Tree: ['>=', 'y', 0]

(assert (not (= x y)))
; Predicate 3: (== x, y) (result: False)
; Tree: ['==', 'x', 'y']

(assert (not (= x y)))
; Predicate 4: (== x, y) (result: False)
; Tree: ['==', 'x', 'y']

(assert (not (= x 0)))
; Predicate 5: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (not (= x 0)))
; Predicate 6: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (not (= y 0)))
; Predicate 7: (== y, 0) (result: False)
; Tree: ['==', 'y', 0]

(assert (not (= y 0)))
; Predicate 8: (== y, 0) (result: False)
; Tree: ['==', 'y', 0]

(assert (= (& x, const) 0))
; Predicate 9: (& x, const) (result: True)
; Tree: ['raw', '(& x#13, const#1)']

(assert (= (& y, const) 0))
; Predicate 10: (& y, const) (result: True)
; Tree: ['raw', '(& y#15, const#1)']

(assert (not (> x y)))
; Predicate 11: (> x, y) (result: False)
; Tree: ['>', 'x', 'y']

(assert (not (> x y)))
; Predicate 12: (> x, y) (result: False)
; Tree: ['>', 'x', 'y']

(assert (not (= (== (>> (- y, x), const), x) 0)))
; Predicate 13: (== (>> (- y, x), const), x) (result: False)
; Tree: ['raw', '(== (>> (- y#15, x#1), const#1), x#1)']

(assert (not (= (== (>> (- y, x), const), x) 0)))
; Predicate 14: (== (>> (- y, x), const), x) (result: False)
; Tree: ['raw', '(== (>> (- y#15, x#1), const#1), x#1)']

(assert (not (= (== (>> (- y, x), const), const) 0)))
; Predicate 15: (== (>> (- y, x), const), 0) (result: False)
; Tree: ['raw', '(== (>> (- y#15, x#1), const#1), const#0)']

(assert (not (= (== (>> (- y, x), const), const) 0)))
; Predicate 16: (== (>> (- y, x), const), 0) (result: False)
; Tree: ['raw', '(== (>> (- y#15, x#1), const#1), const#0)']

(assert (not (= x 0)))
; Predicate 17: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (not (= x 0)))
; Predicate 18: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (= (& (>> (- y, x), const), const) 0))
; Predicate 19: (& (>> (- y, x), const), const) (result: True)
; Tree: ['raw', '(& (>> (- y#15, x#1), const#1), const#1)']

(assert (= (& x, const) 0))
; Predicate 20: (& x, const) (result: True)
; Tree: ['raw', '(& x#1, const#1)']

(assert (= (> (>> (- y, x), const), x) 0))
; Predicate 21: (> (>> (- y, x), const), x) (result: True)
; Tree: ['raw', '(> (>> (- y#15, x#1), const#1), x#1)']

(assert (= (> (>> (- y, x), const), x) 0))
; Predicate 22: (> (>> (- y, x), const), x) (result: True)
; Tree: ['raw', '(> (>> (- y#15, x#1), const#1), x#1)']

(assert (not (= (== (>> (- (>> (- y, x), const), x), const), x) 0)))
; Predicate 23: (== (>> (- (>> (- y, x), const), x), const), x) (result: False)
; Tree: ['raw', '(== (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1)']

(assert (not (= (== (>> (- (>> (- y, x), const), x), const), x) 0)))
; Predicate 24: (== (>> (- (>> (- y, x), const), x), const), x) (result: False)
; Tree: ['raw', '(== (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1)']

(assert (not (= (== (>> (- (>> (- y, x), const), x), const), const) 0)))
; Predicate 25: (== (>> (- (>> (- y, x), const), x), const), 0) (result: False)
; Tree: ['raw', '(== (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), const#0)']

(assert (not (= (== (>> (- (>> (- y, x), const), x), const), const) 0)))
; Predicate 26: (== (>> (- (>> (- y, x), const), x), const), 0) (result: False)
; Tree: ['raw', '(== (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), const#0)']

(assert (not (= x 0)))
; Predicate 27: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (not (= x 0)))
; Predicate 28: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (= (& (>> (- (>> (- y, x), const), x), const), const) 0))
; Predicate 29: (& (>> (- (>> (- y, x), const), x), const), const) (result: True)
; Tree: ['raw', '(& (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), const#1)']

(assert (= (& x, const) 0))
; Predicate 30: (& x, const) (result: True)
; Tree: ['raw', '(& x#1, const#1)']

(assert (= (> (>> (- (>> (- y, x), const), x), const), x) 0))
; Predicate 31: (> (>> (- (>> (- y, x), const), x), const), x) (result: True)
; Tree: ['raw', '(> (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1)']

(assert (= (> (>> (- (>> (- y, x), const), x), const), x) 0))
; Predicate 32: (> (>> (- (>> (- y, x), const), x), const), x) (result: True)
; Tree: ['raw', '(> (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1)']

(assert (= (== (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x) 0))
; Predicate 33: (== (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x) (result: True)
; Tree: ['raw', '(== (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), x#1)']

(assert (= (== (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x) 0))
; Predicate 34: (== (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x) (result: True)
; Tree: ['raw', '(== (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), x#1)']

(assert (not (= x 0)))
; Predicate 35: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (not (= x 0)))
; Predicate 36: (== x, 0) (result: False)
; Tree: ['==', 'x', 0]

(assert (not (= y 0)))
; Predicate 37: (== y, 0) (result: False)
; Tree: ['==', 'y', 0]

(assert (not (= y 0)))
; Predicate 38: (== y, 0) (result: False)
; Tree: ['==', 'y', 0]

(assert (not (= (== (& (| x, y), const), const) 0)))
; Predicate 39: (== (& (| x, y), const), 0) (result: False)
; Tree: ['raw', '(== (& (| x#1, y#15), const#1), const#0)']

(assert (not (= (== (& (| x, y), const), const) 0)))
; Predicate 40: (== (& (| x, y), const), 0) (result: False)
; Tree: ['raw', '(== (& (| x#1, y#15), const#1), const#0)']

(assert (not (= (== (& x, const), const) 0)))
; Predicate 41: (== (& x, const), 0) (result: False)
; Tree: ['raw', '(== (& x#1, const#1), const#0)']

(assert (not (= (== (& x, const), const) 0)))
; Predicate 42: (== (& x, const), 0) (result: False)
; Tree: ['raw', '(== (& x#1, const#1), const#0)']

(assert (not (= (== (& y, const), const) 0)))
; Predicate 43: (== (& y, const), 0) (result: False)
; Tree: ['raw', '(== (& y#15, const#1), const#0)']

(assert (not (= (== (& y, const), const) 0)))
; Predicate 44: (== (& y, const), 0) (result: False)
; Tree: ['raw', '(== (& y#15, const#1), const#0)']

(assert (not (> x y)))
; Predicate 45: (> x, y) (result: False)
; Tree: ['>', 'x', 'y']

(assert (not (> x y)))
; Predicate 46: (> x, y) (result: False)
; Tree: ['>', 'x', 'y']

(assert (not (= (== (- y, x), const) 0)))
; Predicate 47: (== (- y, x), 0) (result: False)
; Tree: ['raw', '(== (- y#15, x#1), const#0)']

(assert (not (= (== (- y, x), const) 0)))
; Predicate 48: (== (- y, x), 0) (result: False)
; Tree: ['raw', '(== (- y#15, x#1), const#0)']

(assert (= (== (& (- y, x), const), const) 0))
; Predicate 49: (== (& (- y, x), const), 0) (result: True)
; Tree: ['raw', '(== (& (- y#15, x#1), const#1), const#0)']

(assert (= (== (& (- y, x), const), const) 0))
; Predicate 50: (== (& (- y, x), const), 0) (result: True)
; Tree: ['raw', '(== (& (- y#15, x#1), const#1), const#0)']

(assert (not (= (== (& (>> (- y, x), const), const), const) 0)))
; Predicate 51: (== (& (>> (- y, x), const), const), 0) (result: False)
; Tree: ['raw', '(== (& (>> (- y#15, x#1), const#1), const#1), const#0)']

(assert (not (= (== (& (>> (- y, x), const), const), const) 0)))
; Predicate 52: (== (& (>> (- y, x), const), const), 0) (result: False)
; Tree: ['raw', '(== (& (>> (- y#15, x#1), const#1), const#1), const#0)']

(assert (not (= (> x, (>> (- y, x), const)) 0)))
; Predicate 53: (> x, (>> (- y, x), const)) (result: False)
; Tree: ['raw', '(> x#1, (>> (- y#15, x#1), const#1))']

(assert (not (= (> x, (>> (- y, x), const)) 0)))
; Predicate 54: (> x, (>> (- y, x), const)) (result: False)
; Tree: ['raw', '(> x#1, (>> (- y#15, x#1), const#1))']

(assert (not (= (== (- (>> (- y, x), const), x), const) 0)))
; Predicate 55: (== (- (>> (- y, x), const), x), 0) (result: False)
; Tree: ['raw', '(== (- (>> (- y#15, x#1), const#1), x#1), const#0)']

(assert (not (= (== (- (>> (- y, x), const), x), const) 0)))
; Predicate 56: (== (- (>> (- y, x), const), x), 0) (result: False)
; Tree: ['raw', '(== (- (>> (- y#15, x#1), const#1), x#1), const#0)']

(assert (= (== (& (- (>> (- y, x), const), x), const), const) 0))
; Predicate 57: (== (& (- (>> (- y, x), const), x), const), 0) (result: True)
; Tree: ['raw', '(== (& (- (>> (- y#15, x#1), const#1), x#1), const#1), const#0)']

(assert (= (== (& (- (>> (- y, x), const), x), const), const) 0))
; Predicate 58: (== (& (- (>> (- y, x), const), x), const), 0) (result: True)
; Tree: ['raw', '(== (& (- (>> (- y#15, x#1), const#1), x#1), const#1), const#0)']

(assert (not (= (== (& (>> (- (>> (- y, x), const), x), const), const), const) 0)))
; Predicate 59: (== (& (>> (- (>> (- y, x), const), x), const), const), 0) (result: False)
; Tree: ['raw', '(== (& (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), const#1), const#0)']

(assert (not (= (== (& (>> (- (>> (- y, x), const), x), const), const), const) 0)))
; Predicate 60: (== (& (>> (- (>> (- y, x), const), x), const), const), 0) (result: False)
; Tree: ['raw', '(== (& (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), const#1), const#0)']

(assert (not (= (> x, (>> (- (>> (- y, x), const), x), const)) 0)))
; Predicate 61: (> x, (>> (- (>> (- y, x), const), x), const)) (result: False)
; Tree: ['raw', '(> x#1, (>> (- (>> (- y#15, x#1), const#1), x#1), const#1))']

(assert (not (= (> x, (>> (- (>> (- y, x), const), x), const)) 0)))
; Predicate 62: (> x, (>> (- (>> (- y, x), const), x), const)) (result: False)
; Tree: ['raw', '(> x#1, (>> (- (>> (- y#15, x#1), const#1), x#1), const#1))']

(assert (not (= (== (- (>> (- (>> (- y, x), const), x), const), x), const) 0)))
; Predicate 63: (== (- (>> (- (>> (- y, x), const), x), const), x), 0) (result: False)
; Tree: ['raw', '(== (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#0)']

(assert (not (= (== (- (>> (- (>> (- y, x), const), x), const), x), const) 0)))
; Predicate 64: (== (- (>> (- (>> (- y, x), const), x), const), x), 0) (result: False)
; Tree: ['raw', '(== (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#0)']

(assert (= (== (& (- (>> (- (>> (- y, x), const), x), const), x), const), const) 0))
; Predicate 65: (== (& (- (>> (- (>> (- y, x), const), x), const), x), const), 0) (result: True)
; Tree: ['raw', '(== (& (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), const#0)']

(assert (= (== (& (- (>> (- (>> (- y, x), const), x), const), x), const), const) 0))
; Predicate 66: (== (& (- (>> (- (>> (- y, x), const), x), const), x), const), 0) (result: True)
; Tree: ['raw', '(== (& (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), const#0)']

(assert (not (= (== (& (>> (- (>> (- (>> (- y, x), const), x), const), x), const), const), const) 0)))
; Predicate 67: (== (& (>> (- (>> (- (>> (- y, x), const), x), const), x), const), const), 0) (result: False)
; Tree: ['raw', '(== (& (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), const#1), const#0)']

(assert (not (= (== (& (>> (- (>> (- (>> (- y, x), const), x), const), x), const), const), const) 0)))
; Predicate 68: (== (& (>> (- (>> (- (>> (- y, x), const), x), const), x), const), const), 0) (result: False)
; Tree: ['raw', '(== (& (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), const#1), const#0)']

(assert (not (= (> x, (>> (- (>> (- (>> (- y, x), const), x), const), x), const)) 0)))
; Predicate 69: (> x, (>> (- (>> (- (>> (- y, x), const), x), const), x), const)) (result: False)
; Tree: ['raw', '(> x#1, (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1))']

(assert (not (= (> x, (>> (- (>> (- (>> (- y, x), const), x), const), x), const)) 0)))
; Predicate 70: (> x, (>> (- (>> (- (>> (- y, x), const), x), const), x), const)) (result: False)
; Tree: ['raw', '(> x#1, (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1))']

(assert (= (== (- (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x), const) 0))
; Predicate 71: (== (- (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x), 0) (result: True)
; Tree: ['raw', '(== (- (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), x#1), const#0)']

(assert (= (== (- (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x), const) 0))
; Predicate 72: (== (- (>> (- (>> (- (>> (- y, x), const), x), const), x), const), x), 0) (result: True)
; Tree: ['raw', '(== (- (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), x#1), const#0)']

(assert (not (= (!= (>> (- (>> (- (>> (- y, x), const), x), const), x), const), (<< x, const)) 0)))
; Predicate 73: (!= (>> (- (>> (- (>> (- y, x), const), x), const), x), const), (<< x, const)) (result: False)
; Tree: ['raw', '(!= (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), (<< x#1, const#0))']

(assert (not (= (!= (>> (- (>> (- (>> (- y, x), const), x), const), x), const), (<< x, const)) 0)))
; Predicate 74: (!= (>> (- (>> (- (>> (- y, x), const), x), const), x), const), (<< x, const)) (result: False)
; Tree: ['raw', '(!= (>> (- (>> (- (>> (- y#15, x#1), const#1), x#1), const#1), x#1), const#1), (<< x#1, const#0))']

(check-sat)
(get-model)
