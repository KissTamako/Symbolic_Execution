; SMTLIB2 export - path constraint
; Generated by PyExZ3
(set-logic QF_BV)

(declare-fun x () Int)
(declare-fun y () Int)

(assert (< 0 y))
; Predicate 0: (< 0, y) (result: True)
; Tree: ['<', 0, 'y']

(assert (< y 10))
; Predicate 1: (< y, const) (result: True)
; Tree: ['<', 'y', 10]

(assert (not (= (== (% x, (+ y, const)), const) 0)))
; Predicate 2: (== (% x, (+ y, const)), const) (result: False)
; Tree: ['raw', '(== (% x#0, (+ y#2, const#1)), const#3)']

(assert (not (= (== (% x, (+ y, const)), const) 0)))
; Predicate 3: (== (% x, (+ y, const)), const) (result: False)
; Tree: ['raw', '(== (% x#0, (+ y#2, const#1)), const#3)']

(check-sat)
(get-model)
